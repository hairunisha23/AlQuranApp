<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.alquranapp">

    <!-- Izin yang dibutuhkan -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:label="Al-Qur'an App"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlQuranApp">

        <!-- Activity utama (pakai Jetpack Compose) -->
        <activity android:name=".ui.MainActivity"
            android:exported="true"
            tools:ignore="MissingClass"> <!-- pastikan exported true untuk launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Service untuk pemutar audio (bisa diubah sesuai nama servicemu) -->
        <service
            android:name="AudioPlayerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            tools:ignore="MissingClass" />

    </application>

</manifest>
